import React from "react";
import styled from "styled-components";

const Stays = styled.p`
  font-weight: normal;
`;
const Details = () => {
  return <Stays>hello</Stays>;
};

export default Details;
